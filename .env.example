# Copy to .env.local before building. The value is embedded in the static files
# because this dashboard calls the DANVIC backend directly from the browser.
NEXT_PUBLIC_BACKEND_API_URL=http://127.0.0.1:3000
NEXT_PUBLIC_DANVIC_APP=admin
